# LANCEFALL — improvement loop devlog

Self-paced `/loop` ("10x keep improving the game, build marvelous creations").
One substantial, verified, committed improvement per iteration. Target ~10.

## Rough roadmap (adapt as I go)
1. Animated deep-space background — parallax starfield + drifting nebula
2. Score popups + kill-streak / combo-tier announcements
3. Game-over build recap (ship + perks) + build-aware share string
4. Daily Challenge surfacing (date, daily best, result)
5. Cosmetic palette themes (shard-unlockable reskins)
6. New content — a 4th ship or 3rd boss
7. Achievements / milestones + a lifetime-stats screen
8. Gated diegetic onboarding tutorial
9. Audio — adaptive boss music + SFX polish
10. Final balance/juice tuning + perf + docs refresh

## Log
<!-- newest last; each entry: iteration, what shipped, commit -->
- **Iteration 1** — Animated deep-space background: parallax twinkling starfield + drifting tinted nebula clouds (additive, combo-reactive heat), screen-space behind the camera so it stays calm. Readability preserved (dim, low-alpha). `render.ts` only.
- **Iteration 2** — Score juice: per-kill floating `+score` popups (tier-colored) and big center combo-milestone announcements (RAMPAGE→FRENZY→CARNAGE→UNSTOPPABLE→GODLIKE→LEGENDARY) with shake + flash + ding. Tier tracked per chain, resets on combo break. `game.ts`, `ui.ts`, `style.css`, `world.ts`.
- **Iteration 3** — Game-over build recap: shows the ship + every perk taken with stack counts (e.g. "LANCE · Long Lance×2, Chain Reaction×3"); the COPY SCORE share string now embeds the build too. `perks.ts` (describeStacks), `save.ts`, `game.ts`, `ui.ts`, `style.css`.
- **Iteration 4** — Daily Challenge surfacing: title caption "Daily Challenge · <date> · your best <n>" and an in-run "◆ DAILY" HUD badge so the seeded run is identifiable. `ui.ts`, `game.ts`, `style.css`.
- **Iteration 5** — Cosmetic palette themes (5: Neon/Sunset/Toxic/Vapor/Mono), shard-unlockable + persisted. Retints player ship, the UI accent (`--cyan`), and the nebula backdrop; enemies/bullets stay fixed for readability. Title palette swatch row. `themes.ts` (new), `render.ts`, `save.ts`, `game.ts`, `ui.ts`, `style.css`.
- **Iteration 6** — Third boss, THE BEACON: a brand-new hazard — a rotating diameter LASER that telegraphs → fires → rests while sweeping; lethal only when active, and dash i-frames phase you through it. Alternates Warden→Weaver→Beacon. Verified lethality (on-beam dies, off-beam safe, dashing safe). `tune.ts`, `types.ts`, `boss.ts`, `render.ts`, `game.ts`, `waves.ts`.
- **Iteration 7** — Achievements (12) + a lifetime-stats screen. Achievements evaluate on game-over against run + lifetime totals, toast on unlock; new STATS title screen shows lifetime totals + a got/locked achievement grid. `achievements.ts` (new, +tests), `save.ts`, `world.ts`, `game.ts`, `ui.ts`, `style.css`. 99 tests.
- **Iteration 8** — Roster 3 → 5 ships: TEMPEST (nimble — fast drift/accel, quick regen, slightly shorter dash) and PHANTOM (knife-edge — 1 stamina segment, fastest regen, 1.6× dash, +speed). Verified in-game stat application. `ships.ts` (+tests, 101 total).
- **Iteration 9** — Adaptive boss audio: a dissonant tritone tension voice fades into the drone while a boss is alive and out on death, plus a triumphant rising stinger when a boss falls. Lifecycle verified (added on spawn, removed on kill, no leaks). `audio.ts`, `game.ts`.
- **Iteration 10** — Wrap-up: docs refreshed to v1.2 (README/DESIGN/CLAUDE.md), backing-store DPR capped at 1.5 for hi-DPI fill-rate headroom, final integrated verification (101 tests, clean build, perf under extreme load, a full bot run reaching the first perk draft, zero console errors). Loop complete. `game.ts`, docs.

— Loop complete (10/10). The game grew from a single ship + one boss into a full roster-and-progression arcade title with 3 bosses, 5 ships, 10 perks, themes, achievements, a daily challenge, an animated cosmos, and adaptive audio.

## Masterpiece push (make it Steam-worthy)
Driven by a multi-agent masterplan audit (`lancefall-masterplan` workflow): enemy/boss/perk/meta/modes/biome/music/UX/difficulty specs + harsh reviewer critique.

- **M2 — Game modes + a winnable ARENA (the #1 reviewer ask: a real "beat it" goal).** New `modes.ts` with a `RunConfig` per mode (replaced the `daily` boolean throughout). Modes: ENDLESS, ARENA (scripted 12-wave gauntlet → VICTORY), DAILY, NIGHTMARE (cranked dials, ×1.75 shards), BOSS RUSH (3 bosses back-to-back). Director rewritten to be mode-aware (time-driven vs scripted arena/bossrush); added a victory flow + VICTORY game-over; mode picker on the title + mode HUD badge; mode shard multipliers; forced-boss spawns. Director progression verified in isolation (Arena: win after 12 waves/3 bosses/6 drafts; Boss Rush: win after 3 bosses). `modes.ts`, `waves.ts`, `boss.ts`, `game.ts`, `ui.ts`, `style.css`.
- **M3 — Three new enemy archetypes (variety the roster lacked).** LANCER (telegraphed sniper: locks aim, shows a dashed laser-sight, fires one fast precise shot — punishes standing still), BOMBER (rushes you, detonates a 14-bullet ring on death — punishes killing it adjacent), WISP (packs of 5 fast tiny chasers in a crescent — multi-kill-dash fodder). Full pipeline: types/tune/enemies AI/render/waves intro+weights/death-effects. Verified render (aim-lines read great) + bomber detonation (0→14 bullets). 4 enemies → 7. `types.ts`, `tune.ts`, `enemies.ts`, `render.ts`, `waves.ts`, `game.ts`.
- **M4 — Fourth boss: THE MIRRORBLADE (dash-duelist).** A red echo of the player that winds up (shows a lunge aim-line), fires a parting fan, then ROCKETS along a committed line — its body lethal mid-lunge — then recovers (vulnerable). Enrages under 50% HP (faster, chained dashes). Dash speed tuned so it can't tunnel at 60fps; you dash *through* it (i-frames) to land hits. Now 4 bosses cycle (Warden→Weaver→Beacon→Mirrorblade); added to Boss Rush. Verified duel collision (contact kills, dashing safe). `types.ts`, `tune.ts`, `boss.ts`, `render.ts`, `waves.ts`, `modes.ts`, `game.ts`.
- **M8 — Biome system (a run that evolves).** Every 45s the run cycles through 5 biomes — THE VOID, EMBERFIELD (rushers), THE LATTICE (ranged), BLOOMGARDEN (swarm), NULLSPACE (fast+shielded) — each retinting the nebula backdrop, biasing the enemy mix, and applying a light speed/shield modifier, with an entry banner. Biome tint overrides the cosmetic theme nebula during play (theme still owns ship/UI). Verified cycling + bias + tint. `biomes.ts` (new), `render.ts`, `waves.ts`, `game.ts`.
- **M6 — Procedural music layer.** A lookahead beat sequencer (112 BPM, 16th grid, scheduled on the audio clock) adds kick + bass + a pentatonic arp over the existing drone — A-minor pentatonic so it's consonant by construction. Layers fade in with run intensity (arp engages as the wave heats up); the arp drops darker during boss fights. Folded into start/stopDrone (game untouched), gated by the music volume slider. Tempo + lifecycle verified (can't audition headlessly). `audio.ts`.
- **M5 — Rich run-summary debrief.** The game-over screen now reads like a real post-run report: a "felled by <cause>" subtitle (bullet / boss bullet / collision / the boss / the beam — tracked through playerDie), a personal-best delta ("+41,978 over your best!"), the full build line (mode · ship · perks), and inline newly-unlocked achievement chips (replacing the old toasts). `game.ts`, `ui.ts`, `style.css`.
- **M7 — Adversarial review of the whole push + fixes.** A 2nd multi-agent review (`lancefall-review-3`) found 12 confirmed issues; fixed: Mirrorblade body was lethal in ALL phases (gated lethality to the lunge — recover is now the real opening; verified); Arena bosses scaled HP by script-index (28/44/62) not appearance ordinal — now 16/20/26 via a unified bossCount (verified); wisp packs bypassed the concurrency cap (now clamped); Head Start desynced Daily RNG (now a separate rng); changing settings un-ducked music (track `ducked`); Boss Rush inter-boss draft could let the next boss spawn first (open draft inside the substep loop); mode-description accuracy. Docs refreshed to v2.0. `game.ts`, `waves.ts`, `audio.ts`, `modes.ts`.
- **M1 — Permanent meta-progression tree.** Shards now buy lasting upgrades from a 12-node UPGRADES shop (Quick Recovery, Long Reach, Keen Edge, Grazer, Momentum, Combo Memory, Iron Will, Scavenger, Treasure Hunter, Fortune=+1 draft card, Head Start=begin with random perks, Second Chance=revive once/run). Nodes apply to base RunStats before ship/perks (deriveStats order: base→meta→ship→perks). Added score/shard multipliers, draftSize, revive tokens, head-start perks. Revive verified (consumes token, clears screen, survives; 2nd hit kills). The "one more run" engine. `meta.ts` (new), `perks.ts`, `save.ts`, `world.ts`, `game.ts`, `ui.ts`, `input.ts`, `style.css`.

## Best-in-class push (v2.1) — depth, variety, feel
Goal: make it the best in its category. Driven by build-depth, in-run variety, and game-feel — the three things the genre's best win on. Each feature implemented → verified (tests + tsc + build + in-browser) → committed; capped by a 2-agent adversarial review (correctness + balance) whose findings were all fixed.

- **Perk evolutions — build-defining fusion capstones.** Stack the right perks high enough and a fusion EVOLUTION unlocks, offered as a guaranteed glowing slot in the next draft (Hades duo-boon style). 6: IMPALER (longreach3+pierce2), SUPERNOVA (chain3+nova2), PERPETUAL (siphon2+timethief1), WRAITH (afterimage2+longreach2), INFERNO (grazeburn2+slipstream1), JUGGERNAUT (secondwind2+chain1). Additive — keep the source perks, stack a dramatic bonus applied LAST in deriveStats (new 4th `evoApply` param). An "EVOLUTION READY" announce fires when a recipe is first met; evolutions thread into the run debrief + share string + a HOW TO PLAY codex. `evolutions.ts` (new, +tests), `perks.ts`, `world.ts`, `game.ts`, `ui.ts`, `style.css`.
- **Elite "Champion" enemies.** A small intensity-scaled chance (capped 2 on screen, none in the first ~minute) promotes an eligible enemy to a Champion: 3× HP, 1.45× size, a pulsing gold crown aura, a "CHAMPION inbound" moment. Killing one rains 6 bonus shard gems + 800 score but detonates a 12-bullet volatile ring — risk on the reward. Mobile/fair singles only (darter/orbiter/lancer/bomber). `tune.ts`, `types.ts`, `world.ts`, `waves.ts` (+tests), `game.ts`, `render.ts`.
- **Game-feel pass.** Dash afterimage trail — crisp ship-silhouette echoes streaking along the dash, growing/brightening toward the tip (the genre's signature "snap"). Dash hits that don't kill now emit an impact spark (gold for champions) so the spear visibly bites tanky enemies. `render.ts`, `game.ts`.
- **Discoverability.** HOW TO PLAY upgraded from a toast to a real modal: 7 mechanics rules + all 6 evolution recipes so players know the build goals to chase. `ui.ts`, `style.css`.
- **Adversarial review + fixes.** 2-agent review (correctness + balance). Fixed: WRAITH afterimage faded wrong (alpha hardcoded /0.5 clamped for >half its life); rollDraftCards off-by-one; IMPALER over-tuned (+3→+2 dmg); PERPETUAL degenerate infinite-dash/permanent-slowmo (refund/regen/slowmo all curbed); WRAITH underwhelming (buffed to its 4-pick cost); elite HP 4×→3×; elite bloomer removed (a tanky turret = un-fun sponge). 115 tests, clean build, zero console errors. `evolutions.ts`, `render.ts`, `tune.ts`, `waves.ts`.
- **Combo heat vignette.** A combo-tier-coloured glow swells at the screen edges from ×10 up (full by ~×70) — euphoric escalation for the core chaining loop. Edges only (inner half-radius clear) so bullet readability is untouched; gated by reduce-flashing. `render.ts`.
- **Riposte — the roster's first defensive perk.** Your dash shatters enemy bullets in a band around the spear (boss shots excepted, so boss fights stay honest); widens per stack; no stamina refund (utility, not an infinite-dash engine). 10 → 11 perks. `perks.ts` (+test), `game.ts` (resolveDashHits), `ui.ts`.
- **AEGIS — 7th evolution (the defensive fortress).** Riposte 2 + Second Wind 2 → a wide bullet-shattering dash wall (+40 shatter), deeper stamina, faster regen, longer graze. Gives the new defensive perk a fusion goal and balances the offence-heavy evolution set. Auto-listed in the codex. `evolutions.ts` (+test), `perks.ts`, `ui.ts`.

— v2.1 shipped: 11 perks + **7 fusion evolutions**, **elite Champions**, a **dash afterimage trail**, **combo heat**, dash-hit sparks, and a **HOW TO PLAY codex** — all built on the three best-in-class levers (build depth, in-run variety, game feel), capped by a 2-agent adversarial review. 116 tests, clean build, zero console errors.

## v3 — "best in its category" expansion (multi-agent workflow driven)
A 5-architect design workflow + tech-lead synthesis produced a sequenced, conflict-aware plan (`V3_PLAN.md`); built sequentially, each step verified + committed; design and review done with dynamic workflows.

- **Phase 0 — shared groundwork.** Save schema versioning (`migrate.ts`, one-time legacy-key migration, zero loss) + a `postApply` stat-pipeline capstone (so heat/relics/mutators compose without fighting over `deriveStats`). Canonical order: base→meta→mutator→ship→perks→evo→postApply→boons.
- **Phase 1 — content.** New enemies **Drifter** (arc-fan zoner) + **Shade** (teleporting ambusher) → 9 archetypes. 5th boss **THE HOLLOW** — an intangible phantom you can only damage by dashing through it during telegraphed Clone-Sync windows, with killable echo clones. Arena → 13-wave/5-boss gauntlet; Boss Rush → 5 bosses; both climax on the Hollow.
- **Phase 2 — variety.** 6 **run mutators** (the Daily picks 1-2 deterministically — Champion Tide / Glass Cannon / Bullet Storm / Fog of War / Berserk / Windfall) + 5 **mid-run events** (Shrine / Gambler / Treasure / Champion Hunt / Cursed Pact, pause-and-choose on a ~75s cadence).
- **Phase 3 — endgame depth.** 🔥 **Heat ascension** (8 levels, up to ×3.2 score), ◈ **build archetypes** (draft-biasing build paths, zero-extra-rng so Daily stays deterministic), ☠ **cursed relics** (6 double-edged draft items on the same chassis), ⧬ **build DNA** (shareable build codec). Ghost-replay cut by the synthesis. Capped by a 2-agent review that caught a **critical dash-lock** (relic+mutator could make dashing unaffordable → now clamped) + balance fixes.
- **Phase 4 — Steam polish.** Adaptive perf (frame-time particle auto-scale), combo-blazing dash trail (4→6 ghosts, whitening at high combo), and a victory-cinematic money moment (concentric shockwaves + fountain + punch-zoom).

— v3.0: **9 enemies + Champions + 5 bosses · Heat ladder · mutators + events · archetypes · relics · build DNA · adaptive perf.** 158 tests, clean build, zero console errors. Remaining: Phase 5 — online leaderboards + cloud daily (Cloudflare Workers backend).

## v4 — "make it more FUN" (autonomous overnight build)
Mandate: full autonomy, make the live alpha more FUN + spectacular. Each feature built → verified (tsc + vitest + build + in-browser via the dev `__lf` hook) → committed; capped by a 4-dimension multi-agent adversarial review whose every confirmed finding was fixed. 164 → 194 tests.

- **OVERDRIVE — a combo-charged ultimate.** Kills + grazes fill a meter; at full, tap F / LB to unleash a time-dilated screen-clearing nova: obliterates non-boss chaff in 360px, shatters every non-boss bullet, locks the combo for 3.2s, +5000×mult score, 18s cooldown. HUD meter (RECHARGING/READY) + a combo-charged player aura + an expanding nova shockwave + a 3-layer hero stinger. Pure `overdrive.ts` (+6 tests). `tune.ts`, `world.ts`, `types.ts`, `input.ts` (F / gamepad LB), `game.ts`, `audio.ts`, `render.ts`, `ui.ts`, `style.css`.
- **THE SOVEREIGN — the 6th & final boss.** A crowned monarch that warps space. Its body is ARMORED (dash deals nothing) while its three orbiting Cores live; dash a Core to shatter it (combo + score + a weak-point chunk to the crown). Break all three → the body cracks EXPOSED for a punish window, then the Cores reform. Every Sovereign bullet curves under a gravity well, bending volleys into sweeping galaxy arms. Two armored phases alternate: a rotating star of CROWN BEAMS (6 spokes) and a golden-angle NOVA SPIRAL. Now the 6th boss in Endless rotation, the Arena finale (15 waves), and Boss Rush; two achievements (Regicide, Long Live the Lance). Pure math in `sovereign.ts` (gravity, beam hit-test, core orbit, phase rules; +14 tests); pattern loop in `boss.ts`; Core orbit in `enemies.ts`. `types.ts`, `tune.ts`, `game.ts`, `render.ts`, `modes.ts`, `achievements.ts`, `waves.ts`, `world.ts`.
- **CLUTCH moments.** **LAST BREATH** — an automatic bullet-time second wind: when a lethal hit lands and it's off cooldown, the game drops into a deep 0.18× slow-mo for 1.7s, grants 0.55s grace, and shoves nearby bullets outward to open an escape lane — it doesn't save you, it gives you the time to save yourself (38s cooldown; sits ahead of the Second Chance revive). **COMBO ERUPTION** — at ×50 / ×100 / … a player-centred nova shatters every non-boss bullet in 360px (breathing room), scorches nearby chaff, and drops a big score bonus; re-arms on a combo break. Pure `clutch.ts` (+9 tests). `tune.ts`, `world.ts`, `scheduler.ts` (deep slow-mo), `audio.ts`, `render.ts` (violet bullet-time vignette), `game.ts`.
- **Adversarial review + fixes (22 agents, 4 dimensions: correctness / balance / performance / edge-cases).** 8 verified findings, all fixed. The headline: the Sovereign's armor invariant was enforced only on the primary dash path, so the afterimage ghost, the OVERDRIVE nova, the OVERDRIVE boss-chunk, and COMBO ERUPTION all bypassed it — now centralised in a shared `spearBlocked()` predicate, with Cores made a dash-only target (no AoE shatter) and boss bullets spared by ERUPTION. Also fixed a real hot-path allocation (`gravityPull` built an object per boss-bullet per frame → inlined at the call site, pure fn kept for tests) and the nova ring drifting off-centre under the burst's punch-zoom (now camera-projected).
- **POWER-UP DROPS.** Bosses always drop a temporary power-up; elite Champions drop one ~40% of the time. Walk over the floating pickup (magnet-assisted) for a timed, build-agnostic buff — one active at a time, a new pickup replaces the current: **OVERREACH** (×2.4 spear reach + longer dash), **HASTE** (speed + cheap fast-regen dashes), **FRENZY** (+3 dash damage, kill-fuel, chains), **GREED** (double score + shards), **AEGIS** (grazing keeps you alive). Rides the existing stat pipeline via one applier in the capstone slot (composes with perks/relics/heat; respects the shardMul cap + grazeRadius floor). HUD badge with a live countdown + a coloured player aura + spinning hexagon pickup orbs + a pickup arpeggio. Pure `powerups.ts` (+11 tests). A focused 2-dimension review (8 agents) caught a **Daily-determinism break** — drops drew from the shared `world.rng` at player-driven kill timing, perturbing the director's spawn stream — now fixed with a **separate per-run `dropRng`** (proven: `world.rng` byte-identical with/without drops). `powerups.ts`, `types.ts`, `tune.ts`, `world.ts`, `game.ts`, `render.ts`, `audio.ts`, `ui.ts`, `style.css`.
- **Cosmetic dash-trail unlocks.** Six selectable trail styles on the title (PULSE / EMBER / FROST / VOID / ACID, plus **CROWN** — a gold prestige trail unlocked by felling THE SOVEREIGN, not shards). Purely cosmetic: a trail restyles the streaking ghost silhouettes + trail particles + dash streaks while the spear line stays combo-coloured (so the combo-tier signal is never lost). No rng, no stats — zero gameplay impact. Pure `trails.ts` (+5 tests); save schema bumped v3→v4 (old saves default-fill PULSE). `trails.ts`, `save.ts`, `migrate.ts`, `render.ts`, `game.ts`, `ui.ts`.
- **Per-boss music themes.** Each of the 6 bosses now layers its own tension chord over the procedural soundtrack and recolours the arpeggio, so every fight sounds distinct: WARDEN (tritone), WEAVER (hypnotic minor-3rd+7th on triangle), BEACON (piercing fifth+octave), MIRRORBLADE (aggressive root+octave square), HOLLOW (eerie detuned minor-2nd+tritone cluster), THE SOVEREIGN (a grand 4-voice chord). Generalised the single tritone boss voice into a per-boss drone CHORD + a per-theme arp-pitch multiplier. Pure `bossThemes.ts` (+6 tests). `bossThemes.ts`, `audio.ts`, `game.ts`.
- **REAVER — a 6th ship (snowball aggression).** A new playstyle: dash-kills refund stamina (+16; base killRefund is 0, so this is its engine) and the spear bites wider (+4), with a longer combo window — but grazing barely refunds stamina (×0.4) and passive regen is sluggish (×0.65). Shreds swarms (chains fuel themselves), weaker in lulls/boss fights — a real risk/reward identity. 3,500-shard unlock. Pure stat profile in `ships.ts` (+1 test); the roster + selector auto-render it.
- **BROODER — a 10th enemy (spawner).** A slow violet carrier pod that periodically hatches a fast mini drone (telegraphed by a pulsing core), up to a cap of 4, then it's a slow target — forcing the decision to prioritise dashing it down before it floods the screen. hp 2, unlocks at t=120, modest weight, not elite-eligible. Distinct from the splitter (which splits on death) — the brooder spawns WHILE alive. `tune.ts`, `types.ts`, `enemies.ts`, `waves.ts` (+test), `world.ts`, `render.ts` (a spinning hex pod with orbiting egg dots).
- **Discoverability pass.** With nine new systems, players had no in-game teaching for them — most critically OVERDRIVE (fired on **F**, invisible otherwise). The HOW TO PLAY codex now covers OVERDRIVE, the ×50 Combo Eruption, power-up drops, Last Breath, the Sovereign's armored-CORES rule, and the cosmetic/ship unlocks; the title legend shows the overdrive key. (The in-run HUD already reads `OVERDRIVE READY [F]` and the boss spawn flashes `SHATTER THE CORES`, reinforcing it in-context.) `ui.ts`.

- **Cumulative adversarial review (10 agents, 3 dimensions).** A final pass over the not-yet-reviewed features (trails / per-boss music / REAVER / BROODER) + ALL cross-feature interactions. Came back clean except the BROODER, which had two real bugs (both fixed): (1) `spawnEnemy` never reset `e.subPhase`, so a brooder recycled into a pool slot left with `subPhase ≥ 4` (by a spent brooder or a boss fan counter) hatched nothing — an inert dud (`spawnBoss` reset it; the regular path didn't); (2) brooder hatches drew from `world.rng` at player-kill timing, desyncing the Daily. Fixed: reset `subPhase` in `spawnEnemy`; deterministic golden-angle hatches + an optional explicit spawn `angle` so death-children draw zero `world.rng` (also applied to the flagged `splitInto`). +3 regression tests. Cross-feature interactions (REAVER × power-ups × OVERDRIVE × Sovereign × BROODER minis) verified clean.
- **THE WARREN — a 6th biome.** A dark violet-teal stage in the evolving-biome cycle that biases toward the priority-target enemies — brooders (×2.0), shades (×1.5), drifters (×1.4) — so deep runs hit a distinct tactical stretch (hunt the spawners before the screen floods). Slots between BLOOMGARDEN and NULLSPACE; the biome cycle is array-driven so it's purely additive. `biomes.ts` (+4 tests in a new `biomes.test.ts`).
- **Daily-determinism hardening.** Completing the dropRng thread: the remaining player-kill-timed draws on the seeded director stream — gem scatter (every kill) and the bomber/elite detonation ring offsets — were routed to the per-run `dropRng`, so kills no longer perturb `world.rng`. The Daily's wave/spawn stream now stays identical for everyone regardless of kill order/speed. (Particles hold the boot rng, not the director rng, so they never touched it; rare mid-run event-*choice* rolls remain genuine player divergence.) Verified a kill draws zero `world.rng`; +1 test. `world.ts`, `game.ts`.
- **Pause-screen build summary (QoL).** The pause screen now shows your current build at a glance — ship (+ heat) and the full stacked build line (evolutions, relics, perks), reusing `buildLine()` + the debrief styling. A roguelite staple: review what you have before resuming. `ui.ts`, `game.ts`, `style.css`.
- **CODEX (bestiary).** A title-screen panel showcasing the whole roster — 11 enemies, the Champion elite, and all 6 bosses — each a colour-coded card with a one-line "what it does + how to beat it." Shows off the depth and aids discoverability. Pure `bestiary.ts` (+4 tests, incl. a guard that every spawnable `ENEMY_DEFS` kind is documented); a modal mirroring HOW TO PLAY. `bestiary.ts`, `ui.ts`, `style.css`.
- **End-to-end QA capstone.** A sustained-run verification (45s + 70s sims via the dev hook): no NaN/crash, a boss spawns + fights, biome cycles, OVERDRIVE/power-ups/Combo Eruption/gems all fire, pools stay far within caps (78/1600 bullets, 9/360 enemies). All green.
- **Achievements for the new mechanics.** Four engagement goals: Unleashed (fire OVERDRIVE), Overcharged (4 OVERDRIVES in a run), Cheating Death (survive a Last Breath), Power Player (5 power-ups in a run). Minimal run-tracking (overdriveUses + powerupsCollected; Last Breath uses already tracked) feeding the gameover ctx. `world.ts`, `game.ts`, `achievements.ts` (+1 test).

— v4.0: **OVERDRIVE ultimate · THE SOVEREIGN (6th boss) · LAST BREATH + COMBO ERUPTION · POWER-UP DROPS · cosmetic dash trails · per-boss music · the REAVER ship · the BROODER enemy · a discoverability pass · THE WARREN biome · Daily-determinism hardening · pause-screen build summary · the CODEX · v4 achievements**, capped by three adversarial-review passes (15 findings fixed) + an end-to-end QA pass. 230 tests, clean build, zero console errors.

## AUDIO OVERHAUL — "fully professional, immersive audio + a banger soundtrack"
Owner-requested deep pass (2026-06-13). The synth engine was clean but sounded "browser-y" and the music was ambient texture, not a song. Built autonomously in 6 verified, committed phases on branch `lastlance`. All cosmetic — zero `world.rng` touches, Daily stays bit-identical (no sim/rng files changed); offline-first preserved; `MUSIC_BPM=112` frozen.

- **A — Production master chain + space.** Master sum → `DynamicsCompressor` (glue) → makeup → tanh brickwall soft-clip → out. Offline-first **convolution reverb** (a stereo exp-decay noise IR built once at init; music finally has space) with music + SFX sends. Music **sub-bus tree** (drums/bass/harmony/lead/boss) so stems can layer and the kick can pump. `audio.ts`, `tune.ts`.
- **B — Warm, click-free, stereo SFX.** New `voice()` primitive: detuned twin-osc warmth → lowpass sweep (kills raw-saw buzz) → click-free envelope (linear to TRUE zero before stop) → `StereoPanner`. Rebuilt the harsh SFX through it. **Positional audio** — kills/explosions pan to where they happen (`panFor(x)`). **Humanization** (±cents pitch + ±gain) from a fixed-seed cosmetic `mulberry32` so a long combo isn't a machine-gun of identical clicks. `audio.ts`, `tune.ts`, `game.ts`.
- **C — Pure transport + score (the keystone).** `musicTransport.ts` (bar/beat coords + macro-form A→A'→B→A, 28-bar/~60s rotation) and `musicScore.ts` (**THE LANCE THEME** — the earworm hook: rising arch → one syncopated LEAP to high D5 → stepwise descent gap-fill → unresolved hang on the 4th; built from PENTA so it can't clash and transposes WHOLE with the combo tier; + the moving-bass Am-F-C-G progression). PURE, no ctx/rng. +25 tests.
- **D — The song comes alive.** A LANCE THEME **lead stem** (fat detuned twin-saw through a coherence-gated gain + coherence-opened filter — the hook is SILENT until a clean run earns it, then blooms in, and lifts with the combo tier so the melody IS the scoreboard). **Moving bass** through the chord loop. **Sidechain pump** (kick ducks the pad — the synthwave breath). **PERC/BREAK** stem (hats + ghost-snare at high heat). The ONE BUS tightened: `setCoherence` pushed every frame beside the render half. `audio.ts`, `tune.ts`, `game.ts`.
- **E — Per-boss motifs + mix states.** Each of the 6 bosses states a recognizable 1-bar lead **motif** that replaces the arena hook during the fight (data in `bossThemes.ts`, +3 tests). `setMixState(combat/menu/overdrive/death)` — a duck now also MUFFLES (master music lowpass); OVERDRIVE is a momentary duck-then-bloom swell. `audio.ts`, `tune.ts`, `game.ts`, `bossThemes.ts`.
- **F — AUDIO LAB.** A dev-only bench at `/audiolab.html` (`npm run dev`): every SFX, the soundtrack driven by COHERENCE/tier/heat sliders, boss-theme swap, mix-state A/B. Not in the prod bundle. Playwright-verified: the whole engine runs with zero runtime errors. `audiolab.html`, `src/audioLab.ts`.

— **358 tests** (+28 new), clean prod build (lab excluded), engine smoke-tested error-free. Hear it: `npm run dev` → game at the dev URL, or `/audiolab.html` for the bench.

### Second soundtrack — SURGE (selectable)
Owner follow-up: a second, more aggressive/energetic soundtrack, selectable in Settings — and "make sure the music is still great when no combo chains are active." New PURE `soundtracks.ts` (TrackProfile + 2 tracks, +8 tests). **AURORA** = the original soul track (unchanged). **SURGE** = aggressive darksynth: driving 8th-note gritty bass, distorted square lead (new `voice()` `drive`/tanh-waveshaper), early square arp + percussion, deeper sidechain pump. Key: SURGE carries an **always-on mid riff** (ungated by coherence) so a zero-combo stretch still grooves — the coherence hook blooms on top as the reward. `playStep` is now fully profile-driven. Settings picker (AURORA/SURGE) + persisted `soundtrack` field (no schema bump — settings spread-default) + audio-lab toggle. Cosmetic; Daily bit-identical. 366 tests, build green.

### Soundtrack complexity + impact pass (research-grounded)
Owner: liked the feel, wanted more complexity + "bang" — researched commercial synthwave/EDM technique and applied it. **Supersaw unison** (voice() stacks N detuned voices + per-voice stereo spread; AURORA 5 / SURGE 7) + an **octave-doubled lead** = huge/fuller leads. A **moving chord pad** (power chords through Am-F-C-G, once/bar, on the pumped harmonyBus) makes the harmony audibly MOVE (always-on → enriches the out-of-combo baseline). **Tension & release**: a riser + rising drum fill in the bar before the macro-form B section, an **impact** on the B downbeat ("the drop") + on boss spawns (deterministic clock / game events, never world.rng). Bigger drums: a layered backbeat **clap**. voice() gained unison/spread/drive; profiles gained unison/spread/padGain/clapHeat. Deployed live. 367 tests, build green, lab smoke-tested error-free. Sources: supersaw/unison (wereformusicians, Syntorial, ModeAudio), voice-leading/counter-melody (Wisseloord), tension&release (EDMProd, Point Blank, ProAudioFiles), darksynth grit (Darksynth wiki, White Bat Audio).
