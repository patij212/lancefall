{
  "name": "lancefall-leaderboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute lancefall --remote --file=schema.sql",
    "stats": "node tools/stats.mjs",
    "test": "node --test tools/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^4.98.0"
  }
}
