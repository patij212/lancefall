# Online leaderboards (optional). Point at your deployed Cloudflare Worker to
# enable the RANKS screen + score submission. Leave unset for a fully offline
# build — the game works exactly the same, just without global boards.
# Deploy the backend from ./worker (see worker/README.md), then:
VITE_LEADERBOARD_URL=
